const initialState = {
  base: {
    subscription: {
      // fill with subscription details (array of objects likely and some others)
    }
  }
}

const mainReducer = (state = initialState, action) => {
  switch (action.type) {

    default: {
      return state
    }
  }
}

export default mainReducer
